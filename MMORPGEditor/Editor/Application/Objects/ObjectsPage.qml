import QtQuick
import MMORPGUIComponents

// Cadastro de novos ObjectModel no catálogo (nome, textura, tipo, tamanho) e
// geração dos JSONs de dados. Ainda não implementado — placeholder de navegação.
Item {
    id: root

    Rectangle {
        anchors.fill: parent
        color: Colors.background0
    }

    Text {
        anchors.centerIn: parent
        text: "Object Creation — WIP"
        font: Fonts.titleMedium
        color: Colors.text
    }
}
