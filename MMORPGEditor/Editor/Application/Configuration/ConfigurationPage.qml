import QtQuick
import MMORPGUIComponents

Item {
    id: root

    Rectangle {
        anchors.fill: parent
        color: Colors.background0
    }

    TextTitle {
        anchors.centerIn: parent
        vText: "Configuration — WIP"
    }
}
